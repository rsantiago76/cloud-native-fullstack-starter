services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - CORS_ORIGINS=http://localhost:5173
      - JWT_SECRET=change_me
      - DATABASE_URL=sqlite:///./app.db
